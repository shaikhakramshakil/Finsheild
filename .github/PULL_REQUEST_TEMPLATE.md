## What
One-sentence summary of the change.

## Why
Motivation / link to issue.

## How
Key changes, by file.

## Verification
- [ ] `pytest -v` passes locally
- [ ] If training-related: smoke-tested with `python -m finsheild.train --model lightgbm --experiment <name>` on synthetic data

## Risk
Any behavior changes, perf impact, backward-compat notes.